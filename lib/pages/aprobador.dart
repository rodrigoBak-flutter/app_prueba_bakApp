import 'package:flutter/material.dart';

class AprobadorPage extends StatelessWidget {
  const AprobadorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Aprobador')),
    );
  }
}
